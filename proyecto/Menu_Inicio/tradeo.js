function TradeoFin() {
    solicitud = math.random() < 0.5;
    cuentas[i].confirmacion = solicitud 
    if (cuentas[i].confirmacion === true) {
        alert("solicitud aceptada")
    } else {
        alert("Solicitud de tradeo ")
    }
}